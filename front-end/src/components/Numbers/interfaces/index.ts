export interface IBet {
    randomValues: any[];
    numbersColor: string;
    maxRange: number;
    maxNumber: number;
    addCart: any;
    betType: string;
    betPrice: number;
    betId: number;
  
  }

export interface INumbers{
    id: string;
    clicked: boolean;
}